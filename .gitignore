# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vite-react-ssg-temp
*.local

# Variantes derivadas das fotos: geradas por `npm run images` (roda no prebuild).
# Os ORIGINAIS em public/images/ continuam versionados — são a fonte da verdade.
public/images/derived/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Claude / planos / docs (nunca versionar) ---
.claude/
CLAUDE.md
**/CLAUDE.md
docs/
# manifesto das skills instaladas — expõe quais skills o Claude usa (seção 8)
skills-lock.json
